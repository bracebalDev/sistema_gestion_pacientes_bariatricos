{
  "name": "ucibam-app",
  "version": "1.0.0",
  "description": "UCIBAM System (React + Express)",
  "main": "server/server.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm run build",
    "server": "cd server && node server.js",
    "client": "cd client && npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "marked": "^18.0.10"
  }
}
